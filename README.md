# witching-hour-platform

Lightweight nested repo for platform-facing models and future creator tooling.

Current intent:

- multi-creator collection registry
- collector progression primitives
- Passport and checkout integration boundaries
- media ingestion manifests for archive scans

This repo starts intentionally small so the main `witching-hour` site can keep moving without locking platform concerns into the app shell.
